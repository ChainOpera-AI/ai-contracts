# AI-Contract
ChainOpera contracts on bsc &amp; opbnb

## Tests

Foundry tests for the payment contracts live in `test/`. Solidity dependencies come from
npm rather than foundry's `lib/`, so install them once first:

```sh
npm install
forge test
```
